# אנליטיקס — מה מותקן ואיך קוראים את זה

## מה מודד

**עמודים** — כל מעבר עמוד נרשם, כולל ניווט פנימי. זה לא מובן מאליו: באתר כזה אין טעינה מחדש בין עמודים, ולכן קוד גוגל רגיל היה רושם רק את העמוד הראשון של כל ביקור.

**לחיצות לבוט** — האירוע `bot_click`, וזה המדד האמיתי. לכל לחיצה נרשם מאיפה היא באה:

| ערך | המקום באתר |
|---|---|
| `plan_weekly` | כפתור החבילה השבועית |
| `plan_monthly` | כפתור החבילה החודשית |
| `plan_two_monthly` | כפתור חבילת החודשיים |
| `chat_input` | תיבת החיפוש בטלפון שבעמוד הבית |
| `floating_button` | הכפתור הצף |
| `cta` | כל שאר הכפתורים |

וגם באיזה עמוד נלחץ, כך שאפשר לראות אם מאמר בבלוג ממיר.

**עוד** — `social_click` לאינסטגרם, `a11y_panel_open` לפאנל הנגישות.

## הפעלה

1. [analytics.google.com](https://analytics.google.com) → `Admin` → `Create` → `Property`
2. שם: Keyreal, אזור זמן ישראל, מטבע שקל
3. Data stream → Web → `https://www.keyreal.co.il`
4. להעתיק את `Measurement ID` — נראה כמו `G-XXXXXXXXXX`
5. ב־Vercel: `Settings` → `Environment Variables`
   - Name: `NEXT_PUBLIC_GA_ID`
   - Value: המזהה
   - `Save`
6. `Deployments` → שלוש נקודות על העליונה → `Redeploy`

**בלי מזהה לא נטען שום סקריפט מעקב.** לא נשמרות עוגיות, ואין חובת באנר.

## מה להגדיר בגוגל אחרי שזה עובד

**לסמן את הלחיצות כהמרה** — בלי זה `bot_click` נשאר אירוע רגיל ולא תראה אחוזי המרה:

`Admin` → `Events` → למצוא `bot_click` → להדליק `Mark as key event`

**לוח שאתה באמת תסתכל עליו** — `Reports` → `Engagement` → `Events`, ולסנן ל־`bot_click`. הפילוח לפי `placement` עונה על השאלה היחידה שחשובה: איזה כפתור עובד ואיזה לא.

## מה לבדוק בשבוע הראשון

- **מאיפה מגיעים** — `Reports` → `Acquisition`. אם רוב התנועה ישירה, החיפוש עוד לא עובד
- **מובייל מול דסקטופ** — `Reports` → `Tech`. הניחוש: 80% מובייל, ולכן שם צריך להיות כל המיקוד
- **איפה עוזבים** — עמוד עם המון כניסות ואפס `bot_click` הוא עמוד שבור, לא עמוד לא מעניין
- **הבלוג ממיר?** — לפלח `bot_click` לפי `page_path`. אם מאמר מביא לחיצות, כדאי לכתוב עוד

## מה שאנליטיקס לא יגיד לך

מי נכנס דרך ChatGPT או Perplexity. הם לא מעבירים מקור הפניה אמין. הדרך היחידה למדוד את זה היא לשאול אותם ישירות מה הם ממליצים — יש על זה סעיף ב־`LAUNCH-CHECKLIST.md`.

## פרטיות

`anonymize_ip` דלוק. אם תרצה מדידה בלי עוגיות בכלל — Plausible או Vercel Analytics עושים את זה, בתשלום קטן, ואז גם אין שאלה של באנר. תגיד ואחליף.
