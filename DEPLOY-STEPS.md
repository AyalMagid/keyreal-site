# העלאה לאוויר — צעד אחר צעד

הקוד מוכן ועבר בנייה נקייה. מכאן זה שלושה דברים: להעלות ל־GitHub, לפרוס ב־Vercel, ולהעביר את הדומיין.

**האתר הקיים ממשיך לעבוד עד שלב 5.** גם אחריו אפשר לחזור אחורה.

---

## מה שצריך להצטייד בו

- חשבון GitHub (חינמי)
- חשבון Vercel (חינמי, מתחברים דרך GitHub)
- גישה לרשם הדומיין — המקום שבו קנית את `keyreal.co.il`
- הגישה לחשבון Formspree (בשביל טופס צור קשר)

---

## שלב 1 — לבנות ולבדוק מקומית

חלץ את ה־ZIP **לתיקייה חדשה**, לא על הקודמת. ואז בטרמינל:

```bash
cd ~/Downloads/next
npm install
npm run build
```

צריך להופיע `✓ Compiled successfully`. אם יש אדום — תעתיק לי ואל תמשיך.

ואז:

```bash
npm run dev
```

פתח `http://localhost:3000` ובדוק:

- חמשת העמודים נפתחים ומיידיים
- כפתור הנגישות נפתח
- לחיצה על כתובת מייל בצור קשר **מעתיקה** ומראה אישור ירוק (לא פותחת תוכנת מייל)
- `localhost:3000/xyz` מציג את עמוד ה־404 המעוצב
- שלושת כפתורי הרכישה מגיעים לטלגרם עם החבילה הנכונה
- **תשלח פנייה אחת בטופס** ותוודא שהגיעה למייל

עצירה: `Ctrl + C`

---

## שלב 2 — להעלות ל־GitHub

1. פתח חשבון ב־[github.com](https://github.com)
2. התקן [GitHub Desktop](https://desktop.github.com) — עושה את זה בלחיצות, בלי פקודות
3. פתח, התחבר
4. `File` → `Add Local Repository` → בחר את התיקייה
5. הוא יציע ליצור repository — אשר, **וסמן Private**
6. `Publish repository`

יש בתיקייה `.gitignore` שמונע העלאת `node_modules` — אל תמחק אותו.

**בדיקה:** באתר github.com אתה רואה את הקוד.

---

## שלב 3 — לפרוס ב־Vercel

1. [vercel.com](https://vercel.com) → `Sign Up` → **Continue with GitHub**
2. `Add New` → `Project`
3. בחר את הריפו → `Import`
4. **אל תשנה שום הגדרה.** לחץ `Deploy`
5. חכה דקה־שתיים

**בדיקה:** תקבל כתובת זמנית כמו `keyreal-site-xxx.vercel.app`. פתח אותה ועבור על הבדיקות משלב 1 — גם מהטלפון.

**האתר באוויר, אבל עדיין לא על הדומיין שלך.**

---

## שלב 4 — אנליטיקס

1. [analytics.google.com](https://analytics.google.com) → צור נכס
2. קח את מזהה המדידה — `G-XXXXXXXXXX`
3. ב־Vercel: `Settings` → `Environment Variables`
   - Name: `NEXT_PUBLIC_GA_ID`
   - Value: המזהה
   - `Save`
4. `Deployments` → שלוש נקודות על הפריסה העליונה → `Redeploy`

בלי מזהה לא נטען שום סקריפט מעקב, ואין חובת באנר עוגיות.

---

## שלב 5 — להעביר את הדומיין

**רק אחרי שהכתובת הזמנית עברה בדיקה מלאה.**

1. ב־Vercel: `Settings` → `Domains`
2. הוסף `www.keyreal.co.il`, ואחר כך גם `keyreal.co.il`
3. Vercel יציג רשומות DNS — בדרך כלל `A` ו־`CNAME`
4. היכנס לרשם הדומיין, לאזור ניהול ה־DNS
5. **צלם מסך של הרשומות הקיימות לפני שאתה משנה.** זה חבל ההצלה
6. החלף לפי מה ש־Vercel מציג
7. המתן — דקות עד 24 שעות

**בדיקה:** `www.keyreal.co.il` מציג את האתר החדש, ובוורסל שתי הכתובות ✓ ירוק.

---

## שלב 6 — מיד אחרי שהדומיין עובד

**Formspree — קריטי.** בחשבון, בטופס, `Settings` → הוסף `www.keyreal.co.il` ו־`keyreal.co.il` לדומיינים המורשים. בלי זה שליחות מהדומיין החדש נדחות, גם אם הטופס עבד מקומית. ואז שלח פנייה אמיתית מהאתר החי.

בדוק שכל אחת מהכתובות נפתחת:

- `/robots.txt`
- `/sitemap.xml`
- `/llms.txt`
- `/terms` → מעביר ל־`/legal`
- `/privacy-policy` → מעביר
- `/cancellation-policy` → מעביר
- `/accessibility` → מעביר

הדבק את `www.keyreal.co.il` בוואטסאפ — צריכה לעלות תמונת השיתוף.

ואז:

1. [Google Search Console](https://search.google.com/search-console) — אמת את הדומיין, הגש `sitemap.xml`
2. [Bing Webmaster Tools](https://www.bing.com/webmasters) — אותו דבר. חשוב: ChatGPT נשען על Bing

---

## שלב 7 — לכבות את Wix

**חכה שבוע.** תן לגוגל לראות שהאתר החדש עובד. אחר כך אפשר לבטל את המנוי.

---

## מכאן, כל שינוי בלי ZIP

תן לי גישה לריפו ואני דוחף שינויים ישר ל־GitHub. Vercel מעלה לבד תוך דקה. אתה לא עושה כלום.

---

## אם משהו נשבר

**האתר לא עולה על הדומיין** → ב־Vercel תחת Domains תראה את השגיאה. בדרך כלל רשומת DNS שלא נשמרה.

**עמוד מסוים שבור** → תגיד לי איזה ומה רואים.

**רוצה לחזור ל־Wix** → החזר את רשומות ה־DNS מהצילום שעשית בשלב 5. חוזר תוך דקות עד שעות.

---

## מה נשאר אחרי ההעלאה

הרשימה המלאה ב־`LAUNCH-CHECKLIST.md`. שני הדברים המשתלמים ביותר:

1. **עמוד לכל עיר** (`/apartments/tel-aviv`) — זה מה שאנשים מקלידים בגוגל, וזה מה שהמתחרה עשה
2. **בדיקה בקורא מסך** — הדבר האחרון שחסר להצהרת הנגישות
